# Mobile art pack — 390 first (Pea Eye)

Nick: ALL HANDS mobile.

## Files (`public/art/mobile/`)
- `masthead_390_compact.png` — type-only, towns locked **Doubs · Adamstown · Buckeystown**
- `divider_390.png` / `.jpg` — parchment-keyed (no checkerboard)
- `page_border_390x844_transparent.png` — phone overlay
- `HERO_390_unit.png` — composed unit with **72px top inset** so title clears the rail

Also mirrored to `mastheads/masthead_dab_energy_trust_390.*` and sitewide `dividers/divider_gilt_electric.*`.

## Call CSS (mobile)
1. Wire compact masthead + parchment divider; never framed masthead.
2. Content inset ≥24–32px from rails; title block starts below top ornament (~56–72px).
3. Cards: **1 column** below 720px.
4. After Hobby redeploy: divider + shell inset + mobile assets.

## QC
One phone unit: parchment + one frame + type + divider. No clip, no checkerboard, no double chrome.
